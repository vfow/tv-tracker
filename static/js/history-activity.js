// History rendering, grouping composition, and pagination are Vue-owned.
// This parser-blocking placeholder remains temporarily to preserve established script order
// until the final legacy frontend file-removal sweep removes the tag and file together.
